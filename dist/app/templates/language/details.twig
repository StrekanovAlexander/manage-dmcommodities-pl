{% extends 'layout.twig' %}

{% block title %}
    Мова: {{ language.short_name }}
{% endblock %}

{% block breadcrumb %}
    <ul class="breadcrumb">
        <li class="breadcrumb-item"><a href="{{ path_for('home.index') }}">Головна</a></li>
        <li class="breadcrumb-item"><a href="{{ path_for('language.index') }}">Мови</a></li>
        <li class="breadcrumb-item active">{{ language.short_name }}</li>
    </ul>
{% endblock %}

{% block content %}
    <h1 class="mb-4">Мова: {{ language.short_name }}</h1>
    <table class="table table-striped table-sm">
        <tr>
            <th>Коротка назва</th>
            <td>{{ language.short_name }}</td>
        </tr>
        <tr>
            <th>Aктуальність</th>
            <td>
                <a href="#" data-toggle="modal" data-target="#modal" data-entity="{{ language.id }}" data-name="{{ language.short_name }}">
                    {{ language.is_actual ? icon.check|raw : icon.times|raw }}
                </a>
            </td>
        </tr>
    </table>
    <div class="mt-4">
        <a class="btn btn-success btn-sm" href="{{ path_for('language.update', {'id': language.id}) }}">Редагувати</a>
    </div>
    <div class="modal fade" id="modal" aria-hidden="true">
        <div class="modal-dialog">
            <form action="{{ path_for('language.actuality') }}" method="POST">
                <div class="modal-content">
                    <div class="modal-header bg-danger">
                        <h5 class="modal-title text-white">Увага!</h5>
                        <button type="button" class="close text-white" data-dismiss="modal">
                            {{ icon.times|raw }}
                        </button>
                    </div>
                    <div class="modal-body text-center h5 my-4"></div>
                    <div class="modal-footer d-flex justify-content-between">
                        <button type="button" class="btn btn-secondary" data-dismiss="modal">Скасувати</button>
                        <button type="submit" class="btn btn-danger">Змінити</button>
                    </div>
                </div>
                <input type="hidden" class="input-hidden" name="id" id="{{ language.id }}">  
                {{ form.csrf|raw }}
            </form>    
        </div>
    </div>
    <script>
        $('#modal').on('show.bs.modal', function (event) {
            var button = $(event.relatedTarget)
            var entity = button.data('entity')
            var name = button.data('name')
            var modal = $(this)
            modal.find('.modal-body').text('Змінити актуальність: ' + name + '?')
            modal.find('.input-hidden').val(entity)
        })
    </script>
{% endblock %}