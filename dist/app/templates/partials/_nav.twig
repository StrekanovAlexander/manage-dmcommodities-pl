<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
    <div class="container">
        <a class="navbar-brand" href="{{ path_for('home.index') }}">
            <img src="https://dmcommodities.pl/img/Logo_DM_fin.svg" alt="DM COMMODITIES SP.Z O.O." style="width: 100px">
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path_for('home.index') }}">Головна</a>
                </li>
                <li class="nav-item dropdown">
                    <button role="button" type="button" class="btn" data-toggle="dropdown"> 
                        Довідники
                    </button>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="#">Валюти</a>
                    </div>
                </li>
            </ul>
            <ul class="navbar-nav">
                {% if auth.user.is_root %}
                    <li class="nav-item dropdown">
                        <button role="button" type="button" class="btn" data-toggle="dropdown"> 
                            {{ icon.cog|raw }}
                        </button>
                        <div class="dropdown-menu">
                            <a class="dropdown-item" href="{{ path_for('user.index') }}">Користувачи</a>
                        </div>
                    </li>
                {% endif %}    
                <li class="nav-item">
                    <span class="nav-link">{{ auth.user.user_name }}</span>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path_for('user.logout') }}">Вихід</a>
                </li>
            </ul>
        </div>
    </div>
</nav>